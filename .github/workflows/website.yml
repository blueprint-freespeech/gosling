name: website

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Gosling
        uses: actions/checkout@v3
        with:
          submodules: true
      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libboost-all-dev tor
          cargo install cargo-tarpaulin
          sudo apt-get install -y doxygen pandoc plantuml tidy ruby-mustache graphviz
      - name: Build Pages
        run: |
          make website-rel-with-deb-info
      - name: Deploy to Github Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: dist/rel-with-deb-info/website
